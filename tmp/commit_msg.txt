figure(dataflow): replace obsolete tool-centric spine with 4-agent pipeline

Per advisory: the dataflow still showed the obsolete PRIM-2 Symbol
Navigator + Prior Memory as the spine, when the actual architecture
is parser -> analyzer -> planner -> translator -> validator.

Restructured the dataflow to show:
- Source Intake stage: parser (Tree-Sitter) + Manifest
- Analyze stage: Analyzer (8B reasoning) emitting 3 documents
- Plan & Slice stage: Planner (Reverse-Topo DAG)
- Translate stage: Translator (4B coding) bounded by Symbol
  Navigator + Prior Memory as context inputs (not spine)
- Validate stage: Target Crate -> Validator cascade -> Weakening
  Guard
- Dashed repair back-edge: validator -> translator routed around
  the bottom of the diagram (mirrors workflow's repair loop)

Routing fixes (10 flows, all showcase-validated):
- Use explicit via arrays with x/y alignment to neighbor segments
- Spread spine corridor at x=620 (right of stage 2 nodes) to
  prevent edge-through-node collisions on stacked nodes
- Route translator -> rust_crate via x=820 (left of validator column)
- Route repair via x=725 corridor below node row
- labelAt positions chosen to avoid label-route-clearance violations

Result: archify validate --quality showcase passes for all 9 checks
on the dataflow; lifecycle unchanged and still passing.

Verification:
- python3 -c 'import json; json.load(open("chunked-translation.dataflow.json"))'
  passes (10 flows, 3 cards)
- archify validate dataflow --quality showcase: ok=true
- archify render dataflow: produces chunked-translation.html
- archify visual-check 1440x900: diagram fits, header controls extend
  past viewport (benign; the diagram content is within bounds)

Refs: structure audit advisory on PRIM-2 obsolescence.
