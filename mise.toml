[tools]
go = "1.26.5"
rust = "latest"

[tasks.dev]
description = "Run MAgHARCM translation with YAML config"
run = "go run ./cmd/MAgHARCM --config config.yml"

[tasks.start]
description = "Run MAgHARCM translation with the configured sample"
run = "go run ./cmd/MAgHARCM --config config.yml"

[tasks.build]
description = "Build binary"
run = "go build -o bin/MAgHARCM ./cmd/MAgHARCM"

[tasks.test]
description = "Run tests"
run = "go test ./..."

[tasks.lint]
description = "Run go vet"
run = "go vet ./..."

[tasks.fmt]
description = "Format Go code"
run = "go fmt ./..."

[tasks.clean]
description = "Clean build artifacts and translated projects"
run = "rm -rf bin/ .artifacts/"
